import React from 'react'

export default function detalleCharacter() {

    const charcaterFound  = characters.find((char) => char.id === charId);

    return (
        <div>
            
        </div>
    )
}
